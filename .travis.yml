language: cpp

branches:
  only:
    - master

compiler:
  - gcc

script:
  - test -d build || mkdir build
  - test -d install || mkdir install
  - cd build
  - cmake -DPCH=0 -DCMAKE_BUILD_TYPE=Release -DLIB_INSTALL_DIR="../install/lib" -DINCLUDE_INSTALL_DIR="../install/include" -DCMAKE_INSTALL_PREFIX=../install ..
  - make -j4

notifications:
  irc:
    channels:
      - "irc.rizon.net#cmangos"
    on_success: never
    on_failure: never
